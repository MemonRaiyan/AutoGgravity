version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: autogravity
      POSTGRES_PASSWORD: autogravity_secret
      POSTGRES_DB: autogravity
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - autogravity_network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autogravity"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - autogravity_network
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: api
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - DATABASE_URL=postgresql://autogravity:autogravity_secret@postgres:5432/autogravity?schema=public
      - REDIS_URL=redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - autogravity_network
    restart: always

  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: dashboard
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://api:4000
    depends_on:
      - api
    networks:
      - autogravity_network
    restart: always

volumes:
  pgdata:
  redis_data:

networks:
  autogravity_network:
    driver: bridge
